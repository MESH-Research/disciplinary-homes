# disciplinary-homes

A WordPress plugin for adding disciplinary homes to a Humanities Commons site.

## Development

To get started, install dependencies with:

```
npm install
composer dump-autoload
```

Then, build the plugin by running:

```
npm run build
```
